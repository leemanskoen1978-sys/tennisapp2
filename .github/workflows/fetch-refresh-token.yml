# Instructies: Token vernieuwen (moet LOKAAL - geen browser in GitHub Actions)
name: Token-instructies

on:
  workflow_dispatch:

jobs:
  instructies:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "=============================================="
          echo "Google Refresh Token vernieuwen"
          echo "=============================================="
          echo ""
          echo "OAuth vereist een browser - dit moet LOKAAL:"
          echo ""
          echo "1. cd Webscraping"
          echo "2. source venv_tennis/bin/activate"
          echo "3. python setup_google_calendar.py"
          echo "4. Autoriseer in de browser, kopieer de nieuwe GOOGLE_REFRESH_TOKEN"
          echo "5. GitHub → Settings → Secrets → GOOGLE_REFRESH_TOKEN → Update"
          echo ""
          echo "Daarna: Tennis Lessen Scraper → Run workflow"
